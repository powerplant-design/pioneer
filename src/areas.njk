---
layout: base.njk
title: Areas We Service | Pioneer PGD
description: Plumbing, gasfitting and drainlaying across Upper Hutt, Lower Hutt and Wellington, local, certified trades serving the whole Hutt Valley.
---

<div class="page | stack-large">
    {% set crumbs = [{ name: "Home", url: "/" }, { name: "Areas", url: "/areas/" }] %}
    {% set heroTitle = "Areas We Service" %}
    {% set heroText = "Based in Upper Hutt and working across the whole Hutt Valley and Wellington. Every area page lists the suburbs we cover, the local conditions that matter, and the services available there." %}
    {% set heroImage = "/img/pioneer-areas.jpg" %}
    {% set heroAlt = "Areas serviced by Pioneer PGD across the Hutt Valley and Wellington" %}
    {% include "partials/hero.njk" %}

    <section class="section">
        <div class="section--container">
            <div class="hub-grid">
                {% for card in areas %}
                <a href="/areas/{{ card.slug }}/" class="hub-card hub-card--{{ card.type }}">
                    <div class="hub-card--img">
                        {% image card.image, card.imageAlt, [400, 800], "(min-width: 800px) 800px, 400px" %}
                    </div>
                    <div class="hub-card--body">
                        <h2>{{ card.name }}</h2>
                        <p>{{ card.intro }}</p>
                        <span class="hub-card--more">View area ›</span>
                    </div>
                </a>
                {% endfor %}
            </div>
        </div>
    </section>

    {% include "partials/who-we-are.njk" %}
</div>